# Hoff

```
fun fibonacci(n):
    if n < 2
        then n
        else 
            let a = n-1
            and b = n-2
            in 
                fibonacci(n-1) + fibonacci(n-2) 
            tel
    fi
```
